
<!--Login screen-->
<body class="background-style">

<div class="jumbotron-login login_text" >
  <div class="container text-center">
    <h1>Vídeo Locadora Imperial</h1>      
    <p>Sistema de Gerenciamento de Locadora</p>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="span4 offset4 well login-form-wellbox opacity_login" >
      <legend>Login</legend>
       <span>
       <!-- login/logout flash message here-->
         <%= flash_messages %>
        </span>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div>
              <%= f.label :email %><br />
              <%= f.email_field :email, autofocus: true %>
            </div>

            <div>
              <%= f.label :senha %><br />
              <%= f.password_field :password, autocomplete: "off" %>
            </div>

            <% if devise_mapping.rememberable? %>
              <div><%= f.check_box :remember_me %> <%= f.label :lembrar %></div>
            <% end %>

            <div>
             <%= button_to 'Entrar', new_admin_session_path, method: :get, class: :'btn btn-primary btn-lg' %>
            </div>  
      <% end %>
        <%= render "devise/shared/links" %>
    </div>
  </div> 
</div>
</div><!-- end of bg_image-->




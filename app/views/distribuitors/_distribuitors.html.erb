 <!-- Table -->
  <table class="table table-striped table-bordered table-hover" cellspacing="0" width="100%">
  <tr>
    <th width="20%">Razão Social</th>
    <th>CNPJ</th>
    <th>Telefone</th>
    <th>Contato</th>
    <th></th>
    <th></th>
    <th></th>
   </tr>

  <% @distribuitors.each do |distribuitor| %>
    <tr>
      <td width="20%"><%= truncate(distribuitor.rsocial, length: 100) %></td>
      <td><%= distribuitor.cnpj %></td>
      <td><%= distribuitor.phone %></td>
      <td><%= distribuitor.contact %></td><!-- strip_tags avoid html being displayed-->
      <td><%= link_to distribuitor, class: "btn btn-success btn-mini" do %><!--<i class="fa fa-search" aria-hidden="true"></i>--> Detalhes<% end %>
      <td><%= link_to edit_distribuitor_path(distribuitor), class: "btn btn-warning btn-mini" do %><!--<i class="fa fa-pencil-square-o" aria-hidden="true"></i> -->Editar <% end %>
      <td><%= link_to distribuitor, method: :delete, data: { confirm: 'Tem certeza?' }, class: "btn btn-danger btn-mini"  do %><!--<i class="fa fa-eraser" aria-hidden="true"></i></i>--> Deletar <% end %>      
    </tr>
  <% end %>
</table> <!--end of table -->


<%= form_with(model: medium, local: true) do |form| %>
  <% if medium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medium.errors.count, "error") %> prohibited this medium from being saved:</h2>

      <ul>
      <% medium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-sm-4 form-group">   
    <%= form.text_field :name, class: 'form-control', placeholder: 'Nome'%>
  </div>

  <div class="col-sm-4 form-group">
    <%= form.text_field :value, class: 'form-control', placeholder: '0.00' %>
  </div>

  <div class="actions">
    <%= form.submit 'Criar Mídia' %>
  </div>
<% end %>

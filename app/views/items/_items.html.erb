 <!-- Table -->
  <table class="table table-striped table-responsive" cellspacing="0" width="100%">
  <tr>
    <th>Serial</th>
    <th>Distribuidor</th>
    <th>Mídia</th>
    <th>Filme</th>
    <th>Filme Título em Português</th>
    <th>Data de Aquisição</th>
    <th></th>
    <th></th>
    <th></th>
   </tr>

  <% @items.each do |item| %>
    <tr>
      <td><%= item.serial %></td>
      <td><%= item.distribuitor.rsocial %></td>
      <td><%= item.medium.name %></td>
      <td><%= item.movie.title %></td>
      <td><%= item.movie.title_br %></td>
      <td><%= l item.aquisiton_date %></td>
      <td><%= link_to item, class: "btn btn-success btn-mini" do %><!--<i class="fa fa-search" aria-hidden="true"></i>--> Detalhes<% end %>
      <td><%= link_to edit_item_path(item), class: "btn btn-warning btn-mini" do %><!--<i class="fa fa-pencil-square-o" aria-hidden="true"></i> -->Editar <% end %>
      <td><%= link_to item, method: :delete, data: { confirm: 'Tem certeza?' }, class: "btn btn-danger btn-mini"  do %><!--<i class="fa fa-eraser" aria-hidden="true"></i></i>--> Deletar <% end %>      
    </tr>
  <% end %>
</table> <!--end of table -->

